package com.comeeatme.batch.domain.repository;

import com.comeeatme.batch.domain.JusoLog;
import org.springframework.data.jpa.repository.JpaRepository;

public interface JusoLogRepository extends JpaRepository<JusoLog, Long> {
}
